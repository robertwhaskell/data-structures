class Node(object):
    def __init__(self, val, next):
        super(Node, self).__init__()
        self.val = val
        self.next = next


class list(object):
        def __init__(self, head):
            super(list, self).__init__()
            self.head = head
